# IT3040 – Assignment 1 (Option 1: Singlish → Sinhala)

## Setup
1. Install Node.js (LTS)
2. Clone the repo
3. Install dependencies:
   npm install
   npx playwright install

## Run tests
npx playwright test --project=chromium --headed
